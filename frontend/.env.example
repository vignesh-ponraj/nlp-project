# Production: set to your deployed API origin (no trailing slash), e.g. https://pivotdrift-api.onrender.com
# Leave unset for local dev — Vite proxies /api → http://127.0.0.1:8000
# VITE_API_BASE=https://your-api.example.com
